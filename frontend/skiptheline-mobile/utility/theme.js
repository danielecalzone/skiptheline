import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ff4c04',
    secondary: '#888',
    error: '#ff4c04',
    success: '#53DD6C'
  },
};